name: Deploy SDK Documentation
on: [workflow_dispatch]
jobs:
  deploy_docs:
    runs-on: ubuntu-latest
    steps:
      - name: Call the API to deploy the documentation
        run: wget ${{ secrets.DEPLOY_DOCUMENTATION_URL_AND_TOKEN }}