    $DocComment$
    $PtrNameOnly$ $Name$($Arguments$) const
     {
        if (this->object == nullptr)
            throw daq::InvalidParameterException();

        auto errorGuard = DAQ_ERROR_GUARD();
        auto errCode = this->object->$NameInInterface$($ArgumentNames$);
        daqCheckErrorGuard(errCode, errorGuard);

        return this->addRefAndReturn();
    }