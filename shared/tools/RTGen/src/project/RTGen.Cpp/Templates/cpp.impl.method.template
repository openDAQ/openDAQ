    $DocComment$
    void $Name$($Arguments$) const
    {
        if (this->object == nullptr)
            DAQ_THROW_EXCEPTION(daq::InvalidParameterException);

        auto errCode = this->object->$NameInInterface$($ArgumentNames$);
        daq::checkErrorInfo(errCode);
    }